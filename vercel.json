{
    "buildCommand": "npm run build",
    "outputDirectory": "dist/client",
    "framework": "remix",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/index"
        }
    ]
}